name: medhealth
description: "Medhealth application."

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  http: ^1.4.0
  provider: ^6.0.5
  shared_preferences: ^2.2.2
  url_launcher: ^6.1.5
  equatable: ^2.0.5
  mailer: ^6.0.0
  cupertino_icons: ^1.0.8
  flutter_svg: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter


  flutter_lints: ^6.0.0

flutter:

  uses-material-design: true
  assets:
    - assets/images/
